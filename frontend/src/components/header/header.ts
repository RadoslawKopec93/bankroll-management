
import {defineComponent} from "vue";
import HeaderButton from "@/components/header/header-button/header-button.vue";

export default defineComponent({
    name: 'Header',
    components: {
      HeaderButton
    },
    setup(){
        return{

        }
    }
})